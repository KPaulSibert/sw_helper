{% block sw_cms_element_${cebName} %}
<div class="sw-cms-element-${name}">
${Object.keys(slots).map(k=>`  <slot name="${k}">{% block sw_cms_element_${cebName}_slot_${k} %}{% endblock %}</slot>`).join('\n')}
</div>
{% endblock %}